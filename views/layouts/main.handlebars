<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hall Pass</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/css/jass.css">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
    <header>
        <p class =  "mt-3 text-center text-light fs-1 font-weight-bold">Hall Pass Grade Portal </p>
    </header>
    <nav class = "text-center mb-3">
        <a href ="/ ">Home</a>
        {{#if loggedIn}}
        <button id="logout" class="btn-no-style">Logout</button>
        {{else}}
        <a href="/login">Login or Sign Up</a>
        {{/if}}
    </nav>
    <main>
        <aside>
            <h2></h2>
        </aside>
        <div class="student">
            {{{body}}}
        </div>
        </main>
        {{#if loggedIn}}
        <script src="/js/logout.js"></script>
        {{/if}}
        <!--Still need to add sections for student/class/grade sections once other handlebars are completed -->
    </main>
</body>

</html>