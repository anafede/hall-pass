<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hall Pass</title>
    <link rel="stylesheet" type="text/css" href="/css/jass.css">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
    <header>
        <h1>Hall Pass Grade Portal</h1>
    </header>
    <nav>
        <a href="/">Home</a>
        {{#if loggedIn}}
        <button id="logout" class="btn-no-style">Logout</button>
        {{else}}
        <a href="/login">Login/Sign Up</a>
        {{/if}}
    </nav>
    <main>
        <aside>
            <h2></h2>
        </aside>
        <div class="student">
            {{{body}}}
        </div>
        </main>
        {{#if loggedIn}}
        <script src="/js/logout.js"></script>
        {{/if}}
        <!--Still need to add sections for student/class/grade sections once other handlebars are completed -->
    </main>
</body>

</html>